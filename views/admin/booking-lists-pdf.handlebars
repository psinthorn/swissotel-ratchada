<div class="container">

    <div class="row">
        {{!-- {{> _msg }} --}}
    </div>


    <h5>Current Reservations Lists</h5>
    <div class="hr-1px"></div>
    <div class="row">
        {{!-- <div class="left"><a href="javascript:genPDF()">Download as PDF</a> </div> --}}
    </div>
    <div class="row">

        {{#if bookings }}

        <table class="striped">
            <thead>
                <tr>
                    <th>
                        <h6>Reservation ID</h6>
                    </th>
                    <th>
                        <h6>Name</h6>
                    </th>
                    <th>
                        <h6>Email Address</h6>
                    </th>
                    {{!-- <th><h6>Booking Made</h6></th> --}}
                    {{!-- <th colspan="2">
                        <h6>View As</h6>
                    </th>
                    <th colspan="3" class="right-align">
                        <h6>Manage</h6>
                    </th> --}}

                </tr>
            </thead>

            <tbody>


                {{#each bookings }}


                <tr>

                    <td>{{ _id }}</td>

                    <td>
                        {{ fname}} {{lname}}
                    </td>

                    <td>
                        {{ email }}
                    </td>

                    {{!-- <td class="center-align">
                        <a href="/admin/reservation/views/{{ id }}">HTML</a>
                    </td>
                    <td class="center-align">
                        <a href="/admin/reservation/pdf/{{ id }}" target="_blank">PDF</a>
                    </td>
                    <td class="right-align">
                        <form action="/admin/reservation/{{ id }}?_method=DELETE" method="post" id="delete-form">
                            <input type="hidden" name="_method" value="DELETE">
                            <button type="submit">Delete</button>
                        </form>



                    </td>
 --}}

                </tr>
                {{/each}}

            </tbody>
        </table>

        {{else}}
        <p>Currently is no reservations available!</p>
        {{/if}}


    </div>
</div>